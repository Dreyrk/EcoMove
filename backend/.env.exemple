# Base de données (exemple local)
DATABASE_URL="postgresql://dev_user:dev_password@postgres:5432/mobility_dev"

# Clé secrète pour JWT (à changer en prod)
JWT_SECRET="dev-secret-key"

# Port de l'API
PORT=4000

# Origine autorisée pour le CORS (frontend local)
CORS_URLS="http://localhost:3000,http://frontend:3000"

# Variables PostgreSQL (service docker postgres)
POSTGRES_USER="dev_user"
POSTGRES_PASSWORD="dev_password"
POSTGRES_DB="mobility_dev"